name: Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

env:
  ACERVO_APP_GROUP_ID: group.intrusive-memory.models

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Step 1: Code Quality
  code-quality:
    name: Code Quality
    runs-on: macos-26

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Check for TODOs in committed code
        run: |
          if git diff origin/main...HEAD | grep -E "^\+.*TODO|^\+.*FIXME"; then
            echo "Warning: New TODOs or FIXMEs detected"
          fi

      - name: Check file sizes
        run: |
          find Sources -type f -name "*.swift" -size +100k -exec echo "Warning: Large file: {}" \;

      - name: Check for print statements
        run: |
          if grep -r "print(" Sources/ --include="*.swift" | grep -v "// print"; then
            echo "Warning: print() statements found in source code"
          fi

  # Step 2: Build and Test on macOS
  # Note: Must use xcodebuild because Metal shaders cannot be compiled with swift build.
  # Package.swift targets macOS 26 (swift-tools-version: 6.2, .macOS(.v26)); the agent stack
  # is macOS-26-only (the macOS-27 FoundationModels custom-provider seam was removed), so this
  # builds and runs on the hosted macos-26 image with its default Xcode 26.
  macos-tests:
    name: macOS Tests
    runs-on: macos-26
    needs: [code-quality]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Show Swift version
        run: swift --version

      - name: Build and test
        run: make test-ci 2>&1 | tee test_output.txt

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: macos-test-results
          path: test_output.txt
          retention-days: 7

  # Step 2b: Integration Tests (CLI binary smoke test + dist artifact)
  # Builds AND runs the macOS-26-target `bruja` binary (make dist + smoke test) on the hosted
  # macos-26 image. No special Xcode selection needed — the image's default Xcode 26 ships the
  # Swift 6.2 toolchain the manifest now targets.
  integration-tests:
    name: Integration Tests
    runs-on: macos-26
    needs: [code-quality]
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Build distributable tarball
        run: make dist

      - name: Verify CLI binary
        run: |
          echo "Checking bruja --version"
          ./bin/bruja --version
          echo "Checking bruja --help"
          ./bin/bruja --help
          echo "Checking bruja query --help"
          ./bin/bruja query --help
          echo "Checking bruja chat --help"
          ./bin/bruja chat --help
          echo "Checking bruja download --help"
          ./bin/bruja download --help

      - name: Verify Metal bundle
        run: |
          test -d ./bin/mlx-swift_Cmlx.bundle || { echo "ERROR: Metal bundle not found"; exit 1; }
          test -f ./bin/mlx-swift_Cmlx.bundle/Contents/Resources/default.metallib || { echo "ERROR: metallib not found"; exit 1; }
          echo "Metal bundle verified"

      - name: Upload dist tarball
        uses: actions/upload-artifact@v7
        with:
          name: bruja-dist
          path: dist/*.tar.gz
          retention-days: 30
